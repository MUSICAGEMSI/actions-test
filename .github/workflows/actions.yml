name: Musical Analytics - Execução Diária

on:
  schedule:
    - cron: '0 12 12 * *'  # Todo dia 12 de cada mês às 12:00 UTC (09h Brasília)
  workflow_dispatch:        # Permite disparo manual
  
jobs:
  run-scripts:
    runs-on: ubuntu-latest
    
    steps:
      - name: Clonar o repositório
        uses: actions/checkout@v4
        
      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          
      - name: Cache de dependências
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-
            
      - name: Instalar dependências básicas
        run: |
          python -m pip install --upgrade pip
          echo "📦 Instalando dependências básicas..."
          
      - name: Instalar dependências do requirements.txt
        run: |
          if [ -f requirements.txt ]; then
            echo "📋 Arquivo requirements.txt encontrado. Instalando..."
            pip install -r requirements.txt
          else
            echo "⚠️ Arquivo requirements.txt não encontrado. Instalando dependências manualmente..."
            pip install supabase pandas python-dotenv matplotlib seaborn openpyxl numpy
          fi
          
      - name: Instalar dependências web scraping
        run: |
          pip install playwright beautifulsoup4 requests
          
      - name: Instalar navegadores do Playwright
        run: |
          python -m playwright install chromium
          
      - name: Criar arquivo de credenciais
        run: |
          echo "🔐 Criando arquivo de credenciais..."
          echo "SUPABASE_URL=${{ secrets.SUPABASE_URL }}" > credencial.env
          echo "SUPABASE_ANON_KEY=${{ secrets.SUPABASE_KEY }}" >> credencial.env
          echo "LOGIN_MUSICAL=${{ secrets.LOGIN_MUSICAL }}" >> credencial.env
          echo "SENHA_MUSICAL=${{ secrets.SENHA_MUSICAL }}" >> credencial.env
          
      - name: Verificar dependências instaladas
        run: |
          echo "🔍 Verificando dependências..."
          python -c "
          import sys
          try:
              import pandas as pd
              print('✅ pandas:', pd.__version__)
          except ImportError as e:
              print('❌ pandas não encontrado:', e)
              sys.exit(1)
          
          try:
              import supabase
              print('✅ supabase: OK')
          except ImportError as e:
              print('❌ supabase não encontrado:', e)
              sys.exit(1)
              
          try:
              from dotenv import load_dotenv
              print('✅ python-dotenv: OK')
          except ImportError as e:
              print('❌ python-dotenv não encontrado:', e)
              sys.exit(1)
          
          print('✅ Todas as dependências OK!')
          "
          
      - name: Verificar conexão Supabase
        run: |
          python -c "
          import os
          from dotenv import load_dotenv
          from supabase import create_client
          
          load_dotenv('credencial.env')
          
          try:
              url = os.environ.get('SUPABASE_URL')
              key = os.environ.get('SUPABASE_ANON_KEY')
              
              if not url or not key:
                  raise Exception('Credenciais Supabase não encontradas')
              
              supabase = create_client(url, key)
              print('✅ Conexão Supabase estabelecida com sucesso!')
              
          except Exception as e:
              print(f'❌ Erro na conexão Supabase: {e}')
              exit(1)
          "
          
      - name: Executar script principal
        run: |
          echo "🚀 Iniciando execução do script..."
          python script_historicoaulas.py
          
      - name: Executar análise musical (se existir)
        run: |
          if [ -f musical_analytics.py ]; then
            echo "📊 Executando análise musical..."
            python musical_analytics.py
          else
            echo "⚠️ Arquivo musical_analytics.py não encontrado"
          fi
        continue-on-error: true
        
      - name: Upload de artefatos gerados
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: relatorios-musicais-${{ github.run_number }}
          path: |
            *.xlsx
            *.png
            *.log
            *.csv
          retention-days: 30
          
      - name: Limpeza de arquivos sensíveis
        if: always()
        run: |
          echo "🧹 Limpando arquivos sensíveis..."
          rm -f credencial.env
          
      - name: Log de sucesso
        if: success()
        run: |
          echo "✅ Execução concluída com sucesso!"
          echo "📅 Data/Hora: $(date)"
          
      - name: Log de erro
        if: failure()
        run: |
          echo "❌ Script falhou. Verificar logs acima."
          echo "📅 Data/Hora: $(date)"
